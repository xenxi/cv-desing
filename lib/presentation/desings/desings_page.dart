import 'package:flutter/material.dart';

class DesingsPage extends StatelessWidget {
  const DesingsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
